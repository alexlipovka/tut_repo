{
  "name": "tut_01",
  "version": "1.0.0",
  "scripts": {
    "start": "vite",
    "buildgh": "vite build --base=/tut_repo/",
    "build": "vite build",
    "serve": "vite preview",
    "build_and_deploy": "pnpm run build && deploy.bat"
  },
  "devDependencies": {
    "vite": "^5.0.12"
  },
  "dependencies": {
    "ol": "latest",
    "proj4": "^2.10.0"
  }
}
